# Assignment3.00 History

Date: 5 May 2026 (5 พฤษภาคม 2569)

## Overview

Migrated AcadeMate from local useState state management to a Redux Toolkit store baseline for Session 2. Added three feature slices, configured the centralized store, and connected the app root to Redux Provider while keeping UI rendering in temporary stub mode as planned for Session 3.

## Reason

Session 2 requires replacing component-local state architecture with RTK-based global state so Redux DevTools can inspect the state tree, action log, diff, and time-travel behavior from a single source of truth.

## Changes

1. Added src/app/store.js
   - Created `configureStore` setup with reducer map: `students`, `courses`, `grades`.
   - Enabled default RTK middleware and automatic Redux DevTools integration in development.

2. Added src/features/students/studentsSlice.js
   - Added hard-coded initial student list (5 items).
   - Added `status: 'idle'` and `error: null` state fields.
   - Added reducers and actions: `addStudent`, `deleteStudent`, `updateStudent`.

3. Added src/features/courses/coursesSlice.js
   - Added initial courses list (4 items).
   - Added reducers and actions: `addCourse`, `deleteCourse`.

4. Added src/features/grades/gradesSlice.js
   - Added empty initial grades list.
   - Added reducers and actions: `addGrade`, `updateGrade`, `deleteGrade`.

5. Updated src/main.jsx
   - Imported `Provider` from `react-redux` and `store` from `src/app/store`.
   - Wrapped `<App />` with `<Provider store={store}>`.

6. Updated src/App.jsx
   - Removed all `useState` logic and Session 1 local handlers.
   - Switched to Session 2 stub rendering mode (`students={[]}`, no-op handlers) to keep UI stable before Session 3 hook integration.
   - Updated page heading to indicate Redux migration stage.

## Validation

- Ran `npm run lint` successfully (no errors).
- Ran `npm run build` successfully.
- Confirmed build artifacts generated by Vite without failure.

## Notes

- This update focuses on store/slice wiring only, matching Session 2 scope.
- UI components are intentionally not connected to `useSelector` / `useDispatch` yet; Session 3 will complete that integration.
- Redux DevTools should now show state shape at startup:
  - `students.list` with 5 entries, `students.status` as `idle`, `students.error` as `null`
  - `courses.list` with 4 entries
  - `grades.list` as empty array
